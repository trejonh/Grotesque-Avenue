<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.horse">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_LOGS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/horse_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/horse_launcher"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Holo.NoActionBar.Fullscreen"
        android:name=".core.HorseApplication">
        <activity
            android:name=".SplashScreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".screens.WelcomeScreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:theme="@style/FullscreenTheme" />
        <activity
            android:name=".screens.LobbyScreenActivity"
            android:label="@string/title_activity_lobby_screen" />
        <activity
            android:name=".screens.SelectAGameScreenActivity"
            android:label="@string/title_activity_select_agame_screen" />
        <activity
            android:name=".screens.gameScreens.ColorMePretty"
            android:label="@string/title_activity_color_me_pretty" />
        <activity
            android:name=".core.SendLog"
            android:label="@string/title_activity_send_log"
            android:textAppearance="@android:style/TextAppearance.Large"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="com.horse.SEND_LOG" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter></activity>
    </application>

</manifest>